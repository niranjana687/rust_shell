#[macro_use] 
extern crate shells;
use std::process::Command;


fn main() {
    /*let (code, stdout, stderr) = fish!("echo '{} + {}' | cat", 1, 3);
    
    assert_eq!(code, 0);
    assert_eq!(&stdout[..], "1+3\n");
    assert_eq!(&stderr[..], ""); */

    str = $(<f1.txt)
    echo "$str" | awk 'for '


}